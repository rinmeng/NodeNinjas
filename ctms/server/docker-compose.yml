version: '3.8'
services:
  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: rootpw
      POSTGRES_USER: root
      POSTGRES_DB: ctms
  app:
    image: ctms
    container_name: ctms
    ports: 
      - 15000:5000